import contents from './contents';
import {connect} from 'react-redux';

const mapStatetoProps = state => ({
  
});

const mapDispatchToProps = dispatch => ({
  
});

export default connect(mapStatetoProps, mapDispatchToProps)(contentsComponent);